#поставить ноду
FROM node 

# создать папку под проект
WORKDIR /hw06-email

# скопировать файл проекта(откуда(. = из коревого файла) и куда(.=/hw06-email - в корневой каталог))
COPY . .

# поставить все пакеты
RUN npm install

# запустить сервер на порте
EXPOSE 3000

# команда для запуска (если несколько слов в команде, то разбмваем каждое слово по отдельности)
CMD ["node", "server"]